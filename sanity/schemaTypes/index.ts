import { expense } from "./expense";
import { category } from "./category";
import { goal } from "./goal";
import { subscription } from "./subscription";

export const schemaTypes = [expense, category, goal, subscription];
